export enum ProfileMessages {
  UpdateSuccess = 'Your profile is successfully updated!',
  SamePassword = 'New Password cannot be same as your current password.',
}

export enum LoginMessages {
  InvalidCredentials = 'Invalid email or password',
  LockedAccount = 'Account locked, too many failed attempts. Please contact the administrator.',
}